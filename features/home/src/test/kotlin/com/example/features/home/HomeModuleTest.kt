package com.example.features.home

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Placeholder unit test so :features:home:testDebugUnitTest and testReleaseUnitTest
 * succeed on CI (e.g. Bitrise) when all module test tasks are run.
 */
class HomeModuleTest {

    @Test
    fun placeholder_passes() {
        assertTrue(true)
    }
}
